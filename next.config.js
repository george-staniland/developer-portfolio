/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media1.giphy.com', 'media2.giphy.com', 'media3.giphy.com', 'media4.giphy.com', 'media0.giphy.com', 'www.datocms-assets.com'],

    }
}

module.exports = nextConfig
